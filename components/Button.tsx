import React from 'react';
import {
  TouchableOpacity,
  Text,
  StyleSheet,
  View
} from 'react-native';

/* 
2 type
- Primary button: Background Red, font color white
- Secondary Button: Background red, font color white, border red
- prop: variant -> "Primary", "Secondary"
- Use TouchableOpacity, View and Text
- Optional: add disabled style with prop
 */

const Button = (props: any) => {
  return null; // TODO

}

export default Button;


const styles = StyleSheet.create({
  primary: {},
  secondary: {},
});